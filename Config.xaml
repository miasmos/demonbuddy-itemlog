<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="137" d:DesignWidth="175" Foreground="{DynamicResource TextBrush}">
    <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <GroupBox Header="Rarity filter">
            <StackPanel>
                <ComboBox SelectedValue="{Binding Path=Settings.Rarity}" SelectedValuePath="Tag" ToolTip="Only log at and above this rarity.">
                    <ComboBoxItem Content="Common+" Tag="0" />
                    <ComboBoxItem Content="Magic+" Tag="1" />
                    <ComboBoxItem Content="Rare+" Tag="2" />
                    <ComboBoxItem Content="Legendary" Tag="3" />
                </ComboBox>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>

